spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    schema: classpath:db/schema-h2.sql
    url: jdbc:mysql://localhost:3306/test?useUnicode=true&serverTimezone=GMT&useSSL=false&characterEncoding=utf8
    username: root
    password: 123456

mybatis-plus:
  global-config:
    db-config:
      capital-mode: true

logging:
  level:
    com.baomidou.samples: debug
